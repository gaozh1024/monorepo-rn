# Recipes

这些 recipe 文件是给 AI 和开发者看的最小参考实现。

- `minimal-bootstrap.tsx`: 最小 `AppProvider` 接入
- `theme-toggle.tsx`: 受控主题切换模式
- `api-auth.ts`: 带 token 头和脱敏 observability 的 API 工厂模式

它们不会自动接入模板运行时，只作为可复制、可引用的参考文件存在。
